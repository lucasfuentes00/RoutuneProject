<resources>
    <!--<style name="WhiteKeyStyle">
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_height">match_parent</item>
        <item name="android:layout_weight">1</item>
        <item name="android:gravity">bottom</item>
        <item name="android:paddingBottom">8dp</item>
        <item name="android:textSize">12sp</item>
        <item name="android:textColor">#000000</item>
    </style>

    <style name="BlackKeyStyle">
        <item name="android:layout_width">36dp</item>
        <item name="android:layout_height">120dp</item>
        <item name="android:gravity">bottom</item>
        <item name="android:paddingBottom">8dp</item>
        <item name="android:textSize">12sp</item>
        <item name="android:textColor">#FFFFFF</item>
    </style>-->

    <!-- Tecla blanca activa -->
    <style name="PianoKeyWhiteEnabled">
        <item name="android:background">@drawable/white_key_enabled</item>
    </style>

    <!-- Tecla blanca desactivada -->
    <style name="PianoKeyWhiteDisabled">
        <item name="android:background">@drawable/white_key_disabled</item>
    </style>

    <!-- Tecla negra activa -->
    <style name="PianoKeyBlackEnabled">
        <item name="android:background">@drawable/black_key_enabled</item>
    </style>

    <!-- Tecla negra desactivada -->
    <style name="PianoKeyBlackDisabled">
        <item name="android:background">@drawable/black_key_disabled</item>
    </style>

</resources>
